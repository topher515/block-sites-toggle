# Block Site Toggle

A super-simple configurable site blocker.

# Configure and Build

- Edit `config.json` to specify which sites to enable for blocking.
- Run `make build` to build your extension into `build` dir

# Install

- Goto `chrome://extensions/`
- Enable "Developer Mode"
- Click "Load Unpacked Extension"
- Select `<repo dir>/build`
